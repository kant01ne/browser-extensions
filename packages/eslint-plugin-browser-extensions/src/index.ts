// eslint-disable-next-line no-restricted-imports
import noNewStorageRule from "./no-new-storage"

module.exports = {
  rules: {
    "no-new-storage": noNewStorageRule
    // our rules will go here
  }
}

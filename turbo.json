{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "build:firefox": {
      "dependsOn": ["^build:firefox"],
      "outputs": ["dist/**"]
    },
    "test": {
      "outputs": [""]
    },
    "package": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "dev:firefox": {
      "cache": false
    }
  }
}
